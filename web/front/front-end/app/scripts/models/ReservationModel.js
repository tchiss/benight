define([
    'underscore',
    'backbone',
    'parse'
], function(_, Backbone, Parse) {
    'use strict';

    var ReservationModel = Parse.Object.extend('Reservation', {
    
  	});

return ReservationModel;
});