define([
    'underscore',
    'backbone',
    'parse'
], function(_, Backbone, Parse) {
    'use strict';

    var PaymentModel = Backbone.Model.extend({

    	initialize: function () {

  		}

  	});

return PaymentModel;
});